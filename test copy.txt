program sumas; var int : i, j, p; int module fact(int j); var char : uno; {return(5)} main(){}
program memo; var int : i, j, p; void module fact(int j); var char : uno;{} int module fact(int j); var char : uno; {return(5)} main(){}
program memo; main(){ if (3<4) then {4+1;} else {3*6;} }
program memo; main(){ write("memo es cool") }
program memo; main(){ read(whatever, hey); }
program memo; main(){ meen = "meen"; }
program memo; main(){ for i = 0 to 3 do { clear();;} }
program memo; main(){ while(2 < i) do { t * 4;} }
program memo; main(){ if (3<4 & x<4) then {4+1;} else {3*6;} }
program memo; main(){  write("meen"); }
program memo; main(){ var int : i; }

program sumas;
var int : i, m, n;
var float : j; 

int module fact(int x, float y); 
var int : a; 
{
    write("func");
    return(a);
}

char module letra(char b); 
var int : c;
{
    i = i + j;
    return(i);
}

void module print(); 
{
    write("memo", i*j, "yo");
}
main(){
    while(2 < i) do {j = j+4;}
}

program sumas;
var int : i, m, n;
var char : a;
var float: x, y;

void module fact(int j); 
var char : o;
{
    m = m*m;
    write("mem", o);
}
main(){
    m = 4*5;
    i = m * (n + (i*i));
    if(i > 3 & i < 10) then{
    n = (1);
    } else{
    i = m * n;
    }
    x = x/y;
    while(x < 3) do { 
        y = x * 4;
        }
    n = 1+578;
}

line(i,m);
point();
circle(y);
arc(y);
penup();
pendown();
color(i,m,n);
size(x);
clear();

program sumas;
var int : i, m, n;
var float: x, y;

int module fact(int j, float x, int m); 
var int : uno; 
{
    uno = i+m;
    return(uno);
}
main(){
    module fact(i, x, m);

program sumas;
var int : i, m, n;
var float: x, y;

float module fact(int j, float x, int m); 
var float : uno; 
{
    uno = 2/3;
    uno = x+y;
    return(3.2);
}
main(){
    
}

program sphere;
var int : x, n;
var int : y;
int module fact(int j);
{
    j = 10;
    if(j > 3) then {
        j = j *10;
    }
    else{
        j = j * 100;
    }
    return(j);
}

void module print();
{
    write(1);
    }

main(){
    x = fact(3);
    x = x +1;
    write(x);
}